import tensorflow as tf
import keras
import numpy

placeholder = tf.placeholder(tf.float32, [69, 69, 420, None])
with tf.Session() as s:
    s.run(placeholder, feed_dict={placeholder: {nigga: black}})
